PREFIX termit: <http://onto.fel.cvut.cz/ontologies/termit/>

CONSTRUCT {
    ?assetType termit:has-count ?count
} WHERE {
    SELECT (COUNT(DISTINCT ?asset) AS ?count) {
        ?asset a ?assetType
    }
}
